# Earlier rectangular 3D sources

`run3D001.py` is the 600 x 300 x 200 mm implementation. The diagnostic patcher
and monitored launchers use the same model. Set their work and production paths
before use.

Only `run3D001_monitored_job_18909391.py` is retained because the monitored job
copies were identical.

`rectangular_plume_3d_trial.py` is a separate 800 x 400 x 450 mm trial. The
full-height implementation is in `../rectangular_3d_quantitative/`.
